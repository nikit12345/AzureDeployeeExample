apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-springboot-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: springboot-app
  template:
    metadata:
      labels:
        app: springboot-app
    spec:
      containers:
      - name: my-springboot-app
        image: myacrrepo1234.azurecr.io/nikit12345/AzureDeployeeExample:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 9090
# service type loadbalancer       
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: springboot-app
    k8s-app: springboot-app
  name: springboot-app
spec:
  ports:
  - name: http
    port: 90
    protocol: TCP
    targetPort: 9090
  type: LoadBalancer
  selector:
    app: springboot-app